# 2025-11-23 UI & Runtime Fix

## 变更内容
- 修复 `App.tsx` 中因 `clearLocalSnapshot` 未定义导致的首屏运行时错误（白屏问题）。
- 为 `App.tsx` 恢复 `clearLocalSnapshot` 函数逻辑，用于清除本地最近一次评估快照并重置 `lastSaved` 状态。

## 影响范围
- 影响页面：应用首页（Intro 步骤）加载。
- 用户体验：修复后应用不再在加载时白屏，评估流程可以正常进行，本地历史记录的清除功能恢复可用。

## 备注
- 此修复仅涉及前端运行时逻辑，不改变量表评分或结果算法。